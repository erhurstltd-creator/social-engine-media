# social-engine-media

Public image host for posts generated by [social-engine](https://github.com/erhurstltd-creator/social-engine).

Images live under `runs/<date>/` and are referenced by Buffer when scheduling posts.
This repo is intentionally public so Buffer's API can fetch the images via
`raw.githubusercontent.com` URLs without authentication.

Nothing else lives here.
